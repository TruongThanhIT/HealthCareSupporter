This branch is used to control code when coding. Every sub-function will be create in each branch. Then they will be merge into develop. After each function develop will be merge into master. Then create new develop branch and repeat that. Finally we will has final code in master.  
